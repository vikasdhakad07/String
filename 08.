Method 01: Remainder on dividing by 11


class Solution 
{ 
    static int xmod11(String x)
	{    
	    // code here
	    int num=0;
      for(int i=0; i<x.length(); i++)
          num=(num*10+(x.charAt(i)-'0'))%11;
      return num;
	}
} 
